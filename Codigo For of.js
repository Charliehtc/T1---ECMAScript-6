
// La sentencia sentencia for...of ejecuta un bloque de código para cada elemento de un objeto iterable, 
//como lo son: String, Array, objetos similares a array.


  const arr1 = [3, 5, 7];
  console.log(arr1[0]);
  console.log(arr1[1]);
  console.log(arr1[2]);
  
  //-----------------------------------------------------------------------------------------
  
  // El for of es un bucle o un ciclo que nos permite recorre un array o un objeto
  
  // Syntax
  // for (variable of iterable)

  const arr = [3, 5, 7];
  for (let i of arr) {
    console.log(i);
  }
